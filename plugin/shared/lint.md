# Skills lint

The mechanical gate for skills in this plugin. The linter checks that the frontmatter parses,
the name format and length, the description length, the body line count, and that every
reference resolves. Judgment stays with the rules files; these limits belong to the script.

The script is maintained outside this plugin. Name the command for your environment here:

- Command: not set in this checkout.

If no command is named above, or the named command cannot run in your environment, treat the
linter as unavailable: say so in the report and continue with the judgment rules. Do not
re-derive the mechanical limits by hand and present them as a lint result.
