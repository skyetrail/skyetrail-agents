# driftline-cli

A small command-line tool.

## Working on this

Install with `npm install`. Build with `npm run build`. Tests run with `npm test`.

Run the pre-commit hooks before you push. CI runs the same checks.
