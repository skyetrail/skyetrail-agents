{
  "name": "mistlark-app",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test",
    "lint": "node --check server.mjs && node --check util.mjs"
  }
}
