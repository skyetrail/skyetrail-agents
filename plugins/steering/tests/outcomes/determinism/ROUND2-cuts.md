[
  "`/Users/pete/workspace/skyetrail-agents/plugins/steering/shared/steering-rules.md` \u2014 edited. STE measurement held exactly: rule cells 52, 0 over cap 20, max 20; prose 175\u2192180 sentences, 3 over cap 25, max 29 \u2014 the same three pre-existing sentences (two condition-test sentences, plus a splitter artifact on the quoted full stop inside `\"State the capability.\"`). No sentence I added is over cap.\n\n## The decision rule I applied\n\nA failing example is copyable when three things hold at once: it is a complete directive, it fills a slot the producing run has to fill anyway, and its \"Bad\" label sits outside the quoted line. The Finish example had all three. The fix is either to describe the fault in prose that fills no slot, or to move the label inside the quoted line so no copy can leave it behind. I used both, picking per example.\n\nI chose description over deletion everywhere I changed something. Deleting leaves an auditor with a rule and no way to recognise the case; describing keeps the recognition and removes the template. Every Good half is intact and still quoted \u2014 the copyable text in each section is now only the text worth copying.\n\n## Changed\n\n**Finish** \u2014 replaced the quoted Bad half with prose:\n\n> The failing shape is a check written against that count. In a code review it reads as one entry per changed file, with the file list as the measure of done. Every entry can be empty of the thing the review was for, and the count still comes out complete.\n\nIt reuses \"a count of the parts the work produced\" from the paragraph directly above, so one term carries one meaning. It still maps onto the shipped prompt's `confirm every file the diff touched appears in it`, so an auditor can still catch it. The observed-failure paragraph lost its \"the first check\" back-reference \u2014 now \"One agent finished on that count.\"\n\n**Scope** (the injection list) \u2014 a complete Scope-slot directive in the same domain as the artifacts these skills produce. Now: \"The failing shape is a category name and then a bare list of kinds, with no membership test and no closing clause. A scope line that names injection and then lists four kinds of it is that shape.\" The reviewer anecdote below it was reworded off its back-reference.\n\n**Calibration** (`Report any secret written to a log.`) \u2014 same class, and the strongest remaining copy risk after Finish, because it reads as a finished Scope or Calibration line. Now: \"The failing shape is a bucket name and nothing else. \"Secrets in logs\" is a bucket name.\" That renders the bucket as a noun phrase, not as an order.\n\n**Voice** \u2014 kept the failing sentence, because a rule about sentence form teaches nothing without the sentence. Moved the label inside the quote instead, matching the `Closed:` / `Not closed:` form the file already uses:\n\n> Bad: A second copy of that list drifts from the first. Then the two files disagree.\n\nAny copy now carries `Bad:` into the artifact.\n\n**Preamble** \u2014 added the convention above `## Outcome`, so a later author does not restore symmetry by writing the Bad halves back in. It records the measured copy as its reason.\n\n## Left alone, with the reason\n\n**Scope's `Not closed: shell commands, file paths, or templates.`** It fails two of the three tests: it is a noun fragment, not a directive, so it fills no slot on its own \u2014 a run would have to compose the surrounding sentence, and composing is where the label re-engages. And its label is already inside the quoted line. It is also half of a minimal pair whose whole teaching is that the two lines differ by one trailing clause; describing one half would cost the contrast. I considered folding it into prose as \"drop the trailing clause and the list is not closed\" and decided against it \u2014 the copy risk it carries is below the cost of losing the pair.\n\n**Voice's passing examples** (`Read that file too.`, `The caller checks that the report is complete.`, `The description states the capability.`) and the inline `\"State the capability.\"` prohibition. The first three are the shapes to copy. The fourth carries its prohibition in the same sentence, so it cannot separate from it.\n\n## One thing worth knowing\n\nThe Voice example survived on a second argument as well as the label move: it is written about this plugin's own internals \u2014 two rule files drifting \u2014 while the Finish, Scope, and Calibration Bad halves were all written in security-review language, the exact domain of the artifacts `writing-agents` produces. Domain match is part of what made them fit a slot. If a later author adds a worked example in the producing domain, it is loaded whatever section it sits in.",
  "All three findings applied to `/Users/pete/workspace/skyetrail-agents/plugins/steering/skills/writing-skills/SKILL.md`. No other file touched. `npm run audit` on the file: 24 checks, 19 pass, 0 fail, 1 advisory (`three-evaluations`, pre-existing, unrelated).\n\n**1. Step 8 split; the subject-knowledge check now runs ahead of the dispatch gate.**\nThe check became a new step 3, placed before the baseline dispatch (now step 4), and it carries its own checklist line: `[ ] 3 Subject knowledge listed before any order applied; the draft checked against that list`. It opens `../../shared/authoring.md` at \"What the rule files carry and what they do not\", writes the list into the record before any section order is applied, and states `This step needs no dispatch. Run it whatever step 4 returns.` The \"check the draft against the list, name every instruction from your own knowledge, put them back\" half and the \"deferring content counts as dropping it\" rule moved with it. The baseline comparison stayed put as step 9, now reading only: put the step 4 output beside your draft and keep the correct, specific subject instructions.\n\nInserting a step renumbered 3\u201312 to 4\u201313. Every cross-reference was updated: the \"What has already failed\" bullets (now naming steps 3 and 9, and step 4 for the deliverable branch), the small-change line (`ticks 0, 1, 2, 12 and 13, and marks 3 to 11 not run`), step 1's case list, step 11's model-and-effort reference, both \"follow the branch in step 4\" pointers, the gate's step 11 table, and the three \"steps 8 to 11\" loop references.\n\n**2. The cannot-dispatch branch now carries no reader judgement.**\n`Where the person still wants the text` is gone. The branch reads:\n\n> **Where you cannot dispatch.** Copy the error text into the record. The person always wants the text, so write it. Run steps 3, 6, 7 and 10 first, because those carry the content rules. Then write the text to `<skill-name>-unverified.md` beside the record, and run step 12 over that file. Mark steps 4, 5, 8, 9, 11 and 13 not run.\n\n`Then stop. You hold no deliverable at that point, and text written past this line is not one.` is deleted \u2014 that sentence is what A3 read as \"write nothing\" while A1 and A2 read the next paragraph as \"write something\". The never-at-the-install-path rule, the blocked report, and the do-not-run-the-task-yourself rule are unchanged.\n\n**3. Every ask now has a cannot-ask branch.**\nThree places said or implied ask:\n- The `cannot tell` routing bullet: added `Where you cannot ask, put that question in your report and stop there.`\n- Step 4's task sentence: added `Where you cannot ask, write one task from the request, and name it in the record and in your report as your own.`\n- The stop list's `The person named no task and will not name one` was the loophole that let a non-interactive run stop anyway; it now reads `You can ask the person for a task, and they refuse to name one.` The neighbouring stop bullet also now points at the branch: `Where you cannot dispatch, follow the branch in step 4 first.`\n\nNote on the repository state: `git diff --stat` also shows `plugins/steering/shared/steering-rules.md` and `plugins/steering/skills/writing-agents/SKILL.md` as modified. Neither was touched by me.",
  "All three fixes are in `/Users/pete/workspace/skyetrail-agents/plugins/steering/skills/writing-agents/SKILL.md`. Verified: `npm run audit -- plugins/steering/skills/writing-agents/SKILL.md` returns 24 checks, 19 pass, 0 fail, 1 advisory (`three-evaluations`, pre-existing). `npm run lint` no longer names this file \u2014 the two sentences my first draft pushed over the 25-word prose cap are split, and the file was clean at HEAD. Nothing outside this file changed.\n\n**Fix 1 \u2014 the finish criterion (step 5, renamed \"Write the finish check, then name the statuses\").** The test now sits where the author applies it, not only in the rule file. Four paragraphs: write the check against the stated outcome and never against a count of the parts produced; then run the test \u2014 describe one run that passes the check and stops short of the outcome, and where you can describe one, the check fails and gets rewritten; the result of that test, or the line saying no such run exists, goes into the record. The test applies to every sentence saying when the work is done, with the outcome statement, the finish check, and each success status named as three such places \u2014 this is the exact spread the produced prompt failed across. One line closes the copy path without depending on how the rule file marks its examples: run the test on any check you copied from an example in a rule file, because a copied sentence reaches your prompt without the words around it. Checklist line 5 now reads `Finish check: no passing run stops short; statuses and obligations; retry limit; partial work`.\n\n**Fix 2 \u2014 subject matter (step 6).** The existing `authoring.md` check stays, and two named failure shapes follow it, each stated as a property so it survives the Voice rules:\n- *The unit of work is smaller than the outcome needs.* List every sentence telling the agent what to read, name the unit each gives, name the unit the outcome needs, widen any smaller one. Carries the observed evidence (changed hunks vs. the whole file) and ties back to step 5 \u2014 an agent meeting the smaller unit in both places reads it as the whole work.\n- *The list of cases is shorter than the one you would write unaided.* Write your own list first, before reading the draft's, then put back every case yours holds and the draft does not. Evidence: one security review prompt dropped eleven vulnerability classes the same model named unaided.\n\n**Fix 3 \u2014 the two unobeyed asks.** Each now has a `Where you cannot ask` branch with what it records instead. Path (step 2): record `no path supplied`, make one directory for the work, put the record and prompt in it, name it in the report, say the person chooses the final path. Call sites (\"Reused, or one call\"): record `call sites not supplied, counted as one`, take the one-call-site branch, and report what a second call site changes as a caller obligation. The stop condition \"The person will not name the call sites\" now points at that branch, so a run with nobody to ask does not read it as a stop.\n\nI also added two bullets to \"What has already failed\", matching that section's existing form \u2014 the copied bad example (pointing at step 5) and the ask with no branch (pointing at step 2 and \"Reused, or one call\").\n\nOne coordination note: the concurrent agent's edit to `shared/steering-rules.md` removed the copyable Bad examples and replaced them with failing-shape descriptions. My step 5 wording refers to \"an example in a rule file\" generically and makes no claim about how any rule file marks its examples, so the two changes do not collide."
]