[
  "Both files written.\n\n**`/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-SKILL.md`** \u2014 the rewritten skill. Frontmatter byte-identical to the current file (verified by diff); name `writing-skills`; description unchanged and satisfying every Discovery rule.\n\nMechanical results, run against the repo's own tooling:\n- `node eng/measure-sentences.mjs`: 0 rule cells over 20 words, 0 prose sentences over 25, longest 24. The current file has one prose sentence at 27.\n- Body 160 lines / 500 limit. Description 452 chars / 1024 limit. Five backtick references, all `../../shared/*.md`.\n- `lintContentsList` runs over a component's references, not the component, so no `## Contents` heading is required.\n- `tests/baselines/<skill-name>.md` is safe to name: the backtick reference check only fires on paths starting with `./` or `../`.\n\n**`/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-NOTES.md`** \u2014 what changed and why, plus the blockers.\n\nThree things need flagging before this lands:\n\n1. **`plugins/steering/shared/authoring.md` does not exist.** It is FILE 1 of the design and was out of scope here. Until it lands, `npm run lint` reports `reference does not resolve: ../../shared/authoring.md`, and the Blocking Context rule fails. This file must not land first.\n\n2. **The do-not-weaken rule lives only in `authoring.md`.** `steering-rules.md` marks it Blocking under **changes something**, which this document meets. I kept the single copy per the design's anti-drift argument and made the pointer name the rule explicitly, but an auditor that does not open `authoring.md` will mark a Blocking fail. Flagging rather than deciding.\n\n3. **I closed a branch the design left open, against the current file's behaviour.** The design dropped the existing \"where subagents are not available, run the task yourself\" clause without replacing it. I added a step 3 branch that stops and records the error text instead of restoring the self-run fallback \u2014 a self-produced baseline is METHOD.md \u00a717's failure mode. That is a capability removal and should be reviewed as one.\n\nI also split the step 9 grading row into a four-item list (the design's single sentence measured 27 words) and added one sentence resolving a contradiction between step 4's taught/candidate test and the gate's rule for a newly-appearing miss.",
  null
]