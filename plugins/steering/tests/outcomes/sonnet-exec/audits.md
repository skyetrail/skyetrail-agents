[
  "## Lint result\n\n`npm run lint` (recorded in the repository's `AGENTS.md` `repo-setup` block, run from `/Users/pete/workspace/skyetrail-agents`) exits clean: `All generated files are up to date.` Seven advisories print, one of them against `plugins/steering/skills/writing-skills/SKILL.md` (1 prose sentence over 25 words).\n\n**That result does not settle the target.** The target sits at `/private/tmp/.../scratchpad/cna/writing-skills-SKILL.md`, outside the repository. `npm run lint -- --explain` reports that the command opens `skills/*/SKILL.md`, `commands/*.md`, `agents/*.md`, top-level `.md` under a plugin's `shared/`, and a plugin's `SUMMARY.md`. The target is none of those, so no check reached it. The checks that did not run: frontmatter hazards, name format and length, description length (limit 1024), body line count (limit 500), reference resolution, and the advisory contents-list check for a file over 100 lines. I did not copy the target into the repository to make the lint reach it, because `shared/lint.md` forbids changing anything to extend a check's reach. I did not re-derive the limits by hand.\n\nThe target's author recorded mechanical results in `writing-skills-NOTES.md` (sentence caps, 160-line body, 452-character description, five backtick references). That record is not confirmed by any lint run over the target's current path. Its one negative claim is confirmed independently: `find` over both trees returns no `authoring.md`.\n\n## Scope\n\nTarget type: a SKILL.md. Conditions met: **always**, **describes work**, **changes something**, **reused**. **hand-off** is not met, because the agent reading this skill sits in the conversation its author is having, so `handoff-rules.md` was not applied. **advisory** is not met. Rule files applied: `steering-rules.md` and `skill-rules.md`.\n\n## Findings\n\nOne row per root cause. The Rule cell names every rule that breaks on that cause, primary first.\n\n| Rule | Severity | Result | Defect or difference | Evidence |\n| --- | --- | --- | --- | --- |\n| SR Context 1 (nothing unresolvable); dependants: SR Context 2, SR Scope 3 (membership test), SR Failure 3 (do-not-weaken), SR Composition 3 (partial work) | Blocking | Fail | **Defect** | `../../shared/authoring.md` does not exist anywhere in the repository or the scratchpad. Named at L13, L35, L106 (by section), L130-131, L154, L159. The agent is told to work down its tests to pick the artifact (L13), to take the objective format from it (L35), to follow its audit rule (L131), and to read it for the stop conditions, the retry limit, the draft's fate, and the ban on easing a gate (L154-155). None of that is reachable. An agent reaching L13 either guesses the artifact or stalls; at L131 it self-audits, which is the plugin's own recorded blocking failure (`tests/baselines/writing-skills.md` L35-41) |\n| SR Context 2 (every fact written out or pointed at) | Blocking | Fail | **Defect** | L45: \"The lint holds the cap, so do not count by hand.\" This repository's lint holds no such cap. `npm run lint -- --explain` lists five checks, none of them a counter check; `grep` over `eng/generate-readmes.mjs` finds no reference to the counter line. The cap's value appears nowhere in the target. The version this replaces stated it inline (\"Three consecutive small changes is the limit. The fourth runs the full loop whatever its size.\"). An agent now raises a number nothing reads, and no change ever triggers the full loop. That is the exact failure `tests/baselines/writing-skills.md` L87-96 records, where the count reached five |\n| SK Evidence 1 (baseline comparison recorded) | Blocking | Fail | **Defect** | `plugins/steering/tests/baselines/writing-skills.md` carries no round for this rewrite. Its last entry reads \"Baseline owed, 2026-08-07 \u2026 The next change to this skill runs the whole loop, whatever its size.\" The target's L8 changes the produces line word for word, which the target's own step 2 test (L39-40) classes as a large change. No `tests/baselines/writing-skills-runs/` directory exists. `writing-skills-NOTES.md` records no baseline run. The rule applies, because this plugin maintains the skill |\n| SR Context 2 (every fact written out or pointed at) | Blocking | Warn | **Defect** | L52-55 and L115-116 tell the agent to dispatch subagents \"in a fresh context\", to forbid installed skills, and to \"Name the model and the effort level in both dispatches\", then to repeat \"at the same model and effort level as step 3\". No tool, no prompt contract, and no values are named, and the plugin's `shared/dispatch-protocol.md` and `shared/handoff-rules.md`, which carry the caller's side and the model-and-effort rule, are named nowhere in the target. Warn, because I cannot tell from the document whether the executing environment supplies a dispatch that accepts a model and an effort level. Defect, because two runs dispatched without a named model are not comparable, and the whole gate rests on that comparison |\n| SK Loading 3 (test material not reachable from the skill) | Important | Fail | **Defect** | L126-128 drops the current version's clause \"Name that location inside the record itself, never in the SKILL.md, which must not link to it.\" The replacement reads only \"and name that location inside the record\". The guard that kept a written SKILL.md from pointing at its own baseline record is gone. An agent following this procedure ships a skill that links `tests/baselines/\u2026`, which breaks SK Loading 3 and the second sentence of SK Evidence 1 for every skill this skill produces |\n| SR Composition 1 (every named hole marked required or carrying a default) | Important | Fail | **Defect** | `<round>` at L58 and inside both commands at L61-62 is never defined, and no step assigns it a value. `<skill-name>`, `<n>`, and `<integer>` carry no required mark and no default. At L117 step 9 says to \"run the same two commands over them\": with the same round the glob counts step 3's files too, so the L65 check (\"The first count must equal the number of runs you claim\") fails on correct work; with a new round nothing says what it is. L88 makes it worse, because a void run's file stays in the directory and is still counted |\n| SK Content 4 (one term for one thing) | Important | Fail | *Difference* | L110-111: \"Move a passage where one step needs it and the other steps do not.\" Here \"step\" means a step of the skill being written. Everywhere else in the document, including L31-32 and L136, \"step\" means one of this document's own steps 1 to 10 |\n| SR Voice 3 (nothing that cannot choose to act takes an action verb) | Important | Fail | *Difference* | L31-32 \"Steps 3 and 4 decide what the skill has to say\"; L54 \"A skill that covers the task would stand in for the model and spoil the measurement\"; L148 \"because the skill introduced it\". A step and a skill cannot choose to act. `steering-rules.md` L211 lists a file and a document among the things that cannot |\n\n**Counts by severity.** Blocking 4. Important 4. Advisory 0.\n\n**Defect or difference.** Defects 6. Differences 2.\n\n**Rules that passed.** SR Outcome 1, 2. SR Context 3 (approaches tried: L91-92 states one), 5. SR Scope 1, 2, 4, 5. SR Method 1, 2, 3, 4. SR Finish 1, 2, 5. SR Failure 1, 2, 5. SR Composition 2. SR Voice 1, 2. SK Discovery 1 to 5 (the frontmatter is byte-identical to the shipped file). SK Boundary 1 to 3. SK Content 1, 2, 3, 5, 6. SK Loading 1 (all five references are named directly by the SKILL.md, so hop distance passes; resolution fails under SR Context 1), SK Loading 2.\n\n**Rules not applicable.** SR Scope 6, SR Finish 3, 4, SR Failure 4, SR Calibration 1 to 4, SR Context 4 (all conditioned on **advisory** or on criteria-only, neither of which the target meets). SK Loading 4 (the skill ships no reference file of its own; the step 2 skip branch sits in the front file, which that rule does not cover).\n\n## The three fixes to make first\n\n1. **Land `shared/authoring.md` before this file, or inline what the pointer promises.** Five rules, four of them blocking, fail on one missing file. The author's own note states this and states the ordering constraint. Confirmed.\n2. **Settle the small-change cap.** Either add the counter check to `eng/generate-readmes.mjs` and name the cap, or restore the stated limit in the file. Today L45 asserts an enforcement that does not exist, which is weaker than the version it replaces.\n3. **Run the full loop and record the round.** The plugin's own record says the next change runs it whatever its size, and this change is large by the target's own test.\n\n## Confirming the author's record\n\n`writing-skills-NOTES.md` is complete on the missing `authoring.md` and on the unenforced counter. It is **incomplete in one place**: it calls the unenforced counter \"harmless but unenforced\". It is not harmless. The previous version enforced the cap by stating it; the target removes the statement and delegates to a check nobody wrote, so the net effect is a removed limit. The note also flags two deliberate departures that are not rule findings and did not enter the table: the no-subagent branch now stops the run where the previous version fell back to a self-run, and the do-not-weaken rule is a pointer rather than a copy.\n\n## Weak-executor check: inferences the text does not state\n\nRequested check, beyond the rule files. Each item quotes the target and names what Sonnet 5 is likely to do instead.\n\n1. **L13-14** \"Read `../../shared/authoring.md` and work down its tests.\" The file is absent, and no branch covers a missing rule file. A weaker model reads the four bullets at L16-19, sees that the person asked for a skill, and continues. It never learns the tests. Separately, `../../shared/` is relative to the SKILL.md's own directory; a weaker model often resolves it against the working directory and reports the file missing for the wrong reason.\n2. **No line says where the new SKILL.md goes.** The document never names an output path, a directory, or a filename convention. A weaker model writes to the working directory root, and the name-matches-directory lint check then fails. Inherited from the current version, not a regression.\n3. **L34-35** \"Write the request into the record at `tests/baselines/<skill-name>.md`.\" At step 1 the skill has no name yet, and the path has no root until step 9 supplies one. A weaker model writes to `./tests/baselines/` under whatever directory it is in, then step 9 tells it a different root.\n4. **L39-40** \"A change is small where the description and the lines saying what the skill produces both stay word for word the same.\" For a new skill there is no prior description, so the test has no referent. A weaker model can read the condition as trivially true and take the L41 branch, \"runs step 10 alone\", skipping both baselines. The next sentence, \"Everything else runs every step\", is the only thing preventing that, and it arrives after the misread.\n5. **L39-40** \"the lines saying what the skill produces\" is not a marked region of any file. A weaker model must find them itself and may compare the wrong lines.\n6. **L52-53, L115** \"Dispatch two subagents \u2026 in a fresh context with no skill loaded\" and \"each in a fresh context with the skill loaded\". No mechanism is given for either. A skill under development is not installed, so \"with the skill loaded\" has no literal execution. A weaker model pastes the draft into the prompt without saying so, or claims the skill was loaded.\n7. **L55** \"Name the model and the effort level in both dispatches.\" No values, and no statement of how a dispatch carries them. A weaker model drops the parameter silently and still reports two comparable runs.\n8. **L57-58, L61-62** `<round>` is undefined. A weaker model invents a value at step 3 and a different one at step 9, or reuses the same one and then quietly widens the glob when the count at L65 comes back as 4.\n9. **L65** \"The first count must equal the number of runs you claim.\" No branch for the case where it does not. A weaker model records the mismatch as a note and continues.\n10. **L80** \"Where a run invoked an installed skill, read that skill.\" Nothing tells the subagent to report which skills it invoked, so the caller usually cannot tell. A weaker model concludes no skill was invoked because none was mentioned.\n11. **L88** \"Either way that run is void. Dispatch a replacement.\" Nothing says what happens to the void run's file, which stays in the directory the L61 command counts.\n12. **L93** \"Order the sections the way `../../shared/steering-rules.md` orders its own.\" That file's contents list runs Conditions, Outcome, Context, Scope, Method, Finish, Failure, Calibration, Composition, Voice. A weaker model reads that as the section list for the skill it is writing and produces headings named Conditions, Calibration, and Voice. The author's own intended order, recorded only in the notes, is different again.\n13. **L110-111** \"Move a passage where one step needs it and the other steps do not.\" As above, \"step\" changes referent here. A weaker model can apply it to this workflow's steps.\n14. **L151-152** \"Where two further passes do not settle it, stop.\" The count's starting point is unstated. A weaker model restarts the count after every change and never reaches the limit.\n15. **L131-132** \"Where the skill no longer answers those words, stop and say which words it dropped.\" This depends on step 1 having written `## Objective` to a path the model could resolve. Item 3 breaks that chain.\n\nFiles referenced: `/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-SKILL.md`, `/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-NOTES.md`, `/Users/pete/workspace/skyetrail-agents/plugins/steering/shared/skill-rules.md`, `/Users/pete/workspace/skyetrail-agents/plugins/steering/shared/steering-rules.md`, `/Users/pete/workspace/skyetrail-agents/plugins/steering/shared/lint.md`, `/Users/pete/workspace/skyetrail-agents/plugins/steering/tests/baselines/writing-skills.md`, `/Users/pete/workspace/skyetrail-agents/plugins/steering/skills/writing-skills/SKILL.md`, `/Users/pete/workspace/skyetrail-agents/AGENTS.md`, `/Users/pete/workspace/skyetrail-agents/eng/generate-readmes.mjs`.",
  "`BLOCKED`\n\n**Audit did not start.** `auditing-skills/SKILL.md` places the stop conditions ahead of the workflow as pre-work gates: \"Where you cannot read the target or a rule file, stop and report `BLOCKED`. Name the file you could not read. Do not audit from memory instead.\"\n\n**File I could not read:**\n`/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-agents-SKILL.md` \u2014 does not exist. The `cna/` directory contains only `writing-skills-SKILL.md`, `writing-skills-NOTES.md`, `bug-report-triage/SKILL.md`, `bug-report-triage/reference/replies.md`, and `tests/baselines/bug-report-triage.md`.\n\n**Not the blocker:** all three rule files read fine \u2014 `/Users/pete/workspace/skyetrail-agents/plugins/steering/shared/skill-rules.md`, `steering-rules.md`, `handoff-rules.md`. So did the audit procedure. The block is the target alone.\n\n**Why I did not substitute a near match.** Two families of candidate exist and neither is safe to assume:\n\n1. `/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-SKILL.md` \u2014 same directory, one word off in the filename, but it declares `name: writing-skills`, a different skill from `writing-agents`. Auditing it would return findings against a document nobody asked about.\n2. Sixteen files named `writing-agents/SKILL.md` elsewhere under the scratchpad (`ab-test/alpha/`, `ab-test/beta/`, `mainfull/`, `snap/HEAD/`, `snap/pre-ste/`, and per-commit directories `0fa59cb`, `109be63`, `19459c8`, `4a0cacb`, `7deb2ae`, `b141edd`, `d015e2e`). These are **not** copies of one file \u2014 spot-checking four of them gave three distinct hashes (`ab-test/alpha` and `mainfull` each unique; `ab-test/beta` and `snap/HEAD` identical). Picking one would be picking a revision at random, and the findings would not transfer to the others.\n\n**The extra weak-model check also did not run**, since it has no target text to quote.\n\n**To unblock:** re-issue with the exact path. If you meant a `writing-agents` revision, name which directory; if you meant the file that is actually in `cna/`, the path is `/private/tmp/claude-501/-Users-pete-Documents-vault/e4aee147-a50d-416a-a868-0345d8e11bca/scratchpad/cna/writing-skills-SKILL.md`."
]