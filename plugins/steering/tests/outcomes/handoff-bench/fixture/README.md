# Bench fixture. Intentionally vulnerable.

This small service exists to score security reviews against a known answer key. The problems in
it are planted on purpose. Do not deploy it, copy patterns from it, or fix it; a fixed fixture
scores nothing.
