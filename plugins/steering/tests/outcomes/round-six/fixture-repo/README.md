# harbour-web

The web client for Harbour.

## Working on this

Install with `npm install`. Build with `npm run build`. Tests run with `npm test`.

Contributors should run the pre-commit hooks before pushing. CI runs the same checks.
